import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    background: {
      default: '#F5F7FA', // Change this to your desired color
      paper: '#ffffff',
    },
    secondary: {
      main: '#F9A825'
    }
  },
});